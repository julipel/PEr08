# RAG Ассистент

Система Retrieval-Augmented Generation (RAG) для создания интеллектуального ассистента на основе собственных документов. Проект поддерживает два режима работы: через OpenAI API и через GigaChat от Сбера.

## 📋 Описание

RAG Ассистент — это система, которая объединяет поиск по векторной базе данных с генерацией ответов через языковые модели. Система позволяет задавать вопросы на основе загруженных документов и получать точные ответы с указанием источников.

### Основные возможности

- 🔍 **Векторный поиск** — быстрый поиск релевантных документов с использованием ChromaDB
- 🤖 **Генерация ответов** — создание ответов через OpenAI GPT или GigaChat
- 💾 **Кеширование** — сохранение ответов для ускорения повторных запросов
- 📊 **Оценка качества** — метрики качества через RAGAS
- 🔄 **Два режима работы** — поддержка OpenAI API и GigaChat

## 🏗️ Архитектура

Проект состоит из двух основных модулей:

### 1. `assistant_api/` — Режим OpenAI API
- Использует OpenAI API для генерации ответов
- Модель по умолчанию: `gpt-4o-mini`
- Требует `OPENAI_API_KEY`

### 2. `assistant_giga/` — Режим GigaChat
- Использует GigaChat API от Сбера
- Требует `GIGACHAT_AUTH_KEY` и `GIGACHAT_RQUID`

### Компоненты системы

- **VectorStore** — управление векторным хранилищем (ChromaDB)
- **RAGPipeline** — основной pipeline обработки запросов
- **RAGCache** — кеширование ответов в SQLite
- **evaluate_ragas.py** — скрипт оценки качества системы

## 🚀 Установка

### Требования

- Python 3.11
- Git

### Шаги установки

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd prompt-5_8
   ```

2. **Создайте виртуальное окружение:**
   ```bash
   py -3.11 -m venv venv_py311
   ```

3. **Активируйте виртуальное окружение:**
   
   **Windows (PowerShell):**
   ```powershell
   .\venv_py311\Scripts\activate
   ```
   
   **Windows (CMD):**
   ```cmd
   venv_py311\Scripts\activate.bat
   ```
   
   **Linux/Mac:**
   ```bash
   source venv_py311/bin/activate
   ```

4. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

5. **Создайте файл `.env` в корне проекта:**
   ```env
   # Для OpenAI API режима
   OPENAI_API_KEY=your-openai-api-key-here
   
   # Для GigaChat режима (опционально)
   GIGACHAT_AUTH_KEY=your-gigachat-auth-key
   GIGACHAT_RQUID=your-gigachat-rquid
   ```

## 📖 Использование

### Запуск в режиме OpenAI API

1. Перейдите в директорию `assistant_api`:
   ```bash
   cd assistant_api
   ```

2. Запустите приложение:
   ```bash
   python app.py
   ```

3. Введите ваш вопрос в консоли. Доступные команды:
   - `exit` или `quit` — выход из приложения
   - `stats` — просмотр статистики системы
   - `clear` — очистка кеша

### Запуск в режиме GigaChat

1. Перейдите в директорию `assistant_giga`:
   ```bash
   cd assistant_giga
   ```

2. Запустите приложение:
   ```bash
   python app.py
   ```

### Оценка качества системы (RAGAS)

Для оценки качества RAG системы используйте скрипт `evaluate_ragas.py`:

```bash
cd assistant_api
python evaluate_ragas.py
```

Скрипт оценивает систему по метрикам:
- **Faithfulness** — точность ответа относительно контекста
- **Context Precision** — точность извлечённого контекста

## 📁 Структура проекта

```
prompt-5_8/
├── assistant_api/          # Режим OpenAI API
│   ├── app.py             # Консольное приложение
│   ├── rag_pipeline.py    # Основной RAG pipeline
│   ├── vector_store.py    # Управление векторным хранилищем
│   ├── cache.py           # Кеширование ответов
│   ├── evaluate_ragas.py  # Оценка качества через RAGAS
│   ├── data/
│   │   └── docs.txt       # Документы для индексации
│   └── chroma_db/         # База данных ChromaDB (создаётся автоматически)
│
├── assistant_giga/         # Режим GigaChat
│   ├── app.py             # Консольное приложение
│   ├── rag_pipeline.py    # Основной RAG pipeline
│   ├── vector_store.py    # Управление векторным хранилищем
│   ├── cache.py           # Кеширование ответов
│   ├── gigachat_client.py # Клиент GigaChat
│   └── data/
│       └── docs.txt       # Документы для индексации
│
├── requirements.txt       # Зависимости проекта
├── .env                  # Переменные окружения (не в репозитории)
├── .gitignore           # Игнорируемые файлы
└── README.md            # Документация
```

## 🔧 Настройка

### Добавление документов

Поместите ваши документы в файл `data/docs.txt` в соответствующей директории (`assistant_api/data/` или `assistant_giga/data/`). Документы должны быть разделены пустыми строками или специальными маркерами.

### Настройка модели

В файле `app.py` можно изменить модель OpenAI:

```python
pipeline = RAGPipeline(
    collection_name="api_rag_collection",
    cache_db_path="api_rag_cache.db",
    data_file="data/docs.txt",
    model="gpt-4o-mini"  # Измените на нужную модель
)
```

### Настройка параметров поиска

В `rag_pipeline.py` можно настроить количество возвращаемых документов:

```python
context_docs = self.vector_store.search(user_query, top_k=3)  # Измените top_k
```

## 📊 Метрики и оценка

Система поддерживает оценку качества через RAGAS:

- **Faithfulness** (0.0-1.0) — насколько ответ соответствует контексту
- **Context Precision** (0.0-1.0) — качество извлечённого контекста

Запустите `evaluate_ragas.py` для получения детального отчёта.

## 🛠️ Технологии

- **Python 3.11** — язык программирования
- **ChromaDB** — векторная база данных
- **OpenAI API** — генерация ответов (GPT модели)
- **GigaChat API** — альтернативный провайдер LLM
- **sentence-transformers** — создание эмбеддингов
- **RAGAS** — оценка качества RAG систем
- **SQLite** — кеширование ответов
- **python-dotenv** — управление переменными окружения

## 📝 Лицензия

Этот проект создан в образовательных целях.

## 🤝 Вклад

Если вы хотите внести вклад в проект:

1. Создайте форк репозитория
2. Создайте ветку для вашей функции (`git checkout -b feature/AmazingFeature`)
3. Зафиксируйте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Отправьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## ⚠️ Важные замечания

- **API ключи**: Никогда не публикуйте файл `.env` с API ключами в репозиторий
- **База данных**: Файлы ChromaDB и кеша не должны попадать в репозиторий (уже добавлены в `.gitignore`)
- **Виртуальное окружение**: Папка `venv_py311` не должна попадать в репозиторий

## 📞 Поддержка

При возникновении проблем:

1. Проверьте, что все зависимости установлены
2. Убедитесь, что API ключи правильно настроены в `.env`
3. Проверьте, что Python 3.11 используется
4. Убедитесь, что файл `data/docs.txt` существует и содержит документы

## 🔄 Обновления

### Версия 1.0
- Базовая функциональность RAG
- Поддержка OpenAI API и GigaChat
- Кеширование ответов
- Оценка качества через RAGAS

---

**Создано с ❤️ для работы с RAG системами**
